# Online Roster

**Deployment: Server-only.** No Solaris Loader is required; an ordinary vanilla 26.1.2 client can use this plugin.

This API `0.6.0` example queries the bounded connected-player snapshot and renders `/who` through a server-owned inventory menu. Copy the directory into the configured plugin root or enable the bundled id `online-roster`.
