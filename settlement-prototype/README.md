# Settlement Prototype

**Deployment: Server-only.** No Solaris Loader is required; an ordinary vanilla 26.1.2 client can use this plugin.

This startup-only example selects the bounded plains-village prototype and declares its building, inhabitant, job, and extension plan. Install it only for a fresh Solaris world with that persisted settlement contract, or enable the bundled id `settlement-prototype`.
