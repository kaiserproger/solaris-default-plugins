# Land Claims

**Deployment: Server-only.** No Solaris Loader is required; an ordinary vanilla 26.1.2 client can use this plugin.

This API `0.6.0` example persists rectangular claims and applies server-authoritative zone protection. Copy the directory into the configured plugin root or enable the bundled id `land-claims`.
